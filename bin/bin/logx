#!/usr/bin/env bash
# Description: Logs usage of a script `logx <script name>`

set -e # Exit on error
set -u # Exit on undefined variable
set -o pipefail # Exit on pipe error


# Log usage
echo "$(date +%s) $1" >> "$HOME/dotfiles/bin/bin/usage.txt"