#!/usr/bin/env bash
# Description: Fuzzy find and delete a git branch

# Record usage
logx "$(basename "$0")"

set -e # Exit on error
set -u # Exit on undefined variable
set -o pipefail # Exit on pipe error


branch=$(fzfb)

unifiedAddHistory "git branch -d $branch"
git branch -d "$branch"